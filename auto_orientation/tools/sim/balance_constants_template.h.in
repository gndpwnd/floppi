// AUTO-GENERATED by tools/sim/brute_tune.py — DO NOT HAND-EDIT
// Regenerate with:
//   python3 tools/sim/brute_tune.py --mode {mode} --budget {budget} \
//                                   --output <this-file>
//
// Generated:    {timestamp}
// Generator:    brute_tune.py v{generator_version}
// Search:       mode={mode}  budget={budget}  seed={seed}
// Plant preset: {plant_preset}
// Fitness:      {fitness:.3f}   ({tip_time_s:.3f}s balanced, tip={tipped})
// Trial:        init_perturbation={init_perturbation_deg}deg,
//               disturbance={disturbance_mag}
//
// Reference for context (SelfBallancingRobot3.ino, manually-tuned working bot):
//   Kp=65  Ki=12  Kd=38  PITCH_OFFSET=-8.6  PWM_MAX=255
#pragma once

namespace balance {{

// PID gains
constexpr float KP = {kp:.4f}f;
constexpr float KI = {ki:.4f}f;
constexpr float KD = {kd:.4f}f;

// Mechanical mounting offset — subtracted from the raw IMU pitch BEFORE the PID.
// Equivalent to PITCH_OFFSET in the reference sketch.
constexpr float PITCH_OFFSET_DEG = {offset:.4f}f;

// Motor PWM ceiling (output is clamped to [-PWM_MAX, +PWM_MAX]).
constexpr int PWM_MAX = {pwm_max};

// PID sample period in milliseconds (kept fixed at the reference 5 ms / 200 Hz).
constexpr int PID_SAMPLE_MS = {pid_sample_ms};

// Minimum PWM (stiction floor) — output is snapped to ±MIN_PWM when nonzero
// and below this magnitude. Same as the reference sketch's 15-unit deadband.
constexpr int MIN_PWM = {min_pwm};

}}  // namespace balance
